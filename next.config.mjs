/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['aceternity.com','assets.aceternity.com','images.unsplash.com', 'drive.google.com',"api.microlink.io"],
      },

};

export default nextConfig;
